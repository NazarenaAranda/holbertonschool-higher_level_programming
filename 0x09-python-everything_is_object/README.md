type() Si se pasa un solo argumento (objeto) a type() incorporado, devuelve el tipo del objeto dado.
------------------------------------------------------------------------------------------------------
La función id () La función incorporada id () recibe como argumento un objeto y retorna otro objeto que sirve como identificador único para el primero.
---------------------------------------------------------------------------------------------------------------------
Los dos signos de igual "==" representan la comparación, si yo imprimo esto me va a imprimir un booleano, esto vendría aser True o False
--------------------------------------------------------------------------
Con "is" pasa lo mismo pero sólo si son de tipo string, con numeros no agarra la comparacion y por lo tanto es false
---------------------------------------------------------------------------------------------------------------------

